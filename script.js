function changeStyle(sheet) {
    document.getElementById('theme-stylesheet').setAttribute('href', sheet);
  // La función changeStyle() cambia el atributo href de la hoja de estilo que se está usando (#theme-stylesheet) para aplicar el nuevo archivo CSS seleccionado.
}
